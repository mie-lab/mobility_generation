misc:
  dataset: mobis

  use_pretrain: False
  pretrain_filepath: mobis_movesim_1702300660


  generate_len: 50
  previous_day: 7

  num_gen_samples: 4096
  g_step: 2
  d_step: 2
  k_d: 2
  # 
  if_embed_time: False
  if_embed_user: True
  if_embed_duration: False

  verbose: True
  debug: False
  d_batch_size: 128
  g_batch_size: 32
  print_step: 100
  num_workers: 0
  world_size: 1
  

embedding:
  base_emb_size: 128
  hidden_dim: 512

model:
  networkName: movesim
  #
  layer: 4
  head: 16
  task: attention
  periodic_loss: 0.5
  distance_loss: 0.5

optimiser:
  optimizer: Adam
  max_epoch: 100
  adv_max_epoch: 100
  pre_lr: 0.001
  d_lr: 0.00001
  g_lr: 0.0001
  weight_decay: 0.000001

  rollout_num: 2

  # for decay
  patience: 3
  lr_step_size: 1
  lr_gamma: 0.1

dataset:
  # temp_save_root: /cluster/scratch/hongy/generation/data
  # save_root: /cluster/scratch/hongy/generation/runs
  temp_save_root: ./data
  save_root: ./runs